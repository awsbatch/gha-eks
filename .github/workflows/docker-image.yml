name: Kubernets CI
on:
 push:
  branches:
    - main
jobs:
  pyapp:
   runs-on: ubuntu-latest
   steps:
     - name: Code checkout
       uses: actions/checkout@v4
     - name: Build Docker Image
       run: "docker build -t ravendrarana/pyapp:$GITHUB_SHA ."
